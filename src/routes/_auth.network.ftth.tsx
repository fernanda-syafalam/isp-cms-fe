import { createFileRoute } from '@tanstack/react-router'

// Route stub only — component is code-split into _auth.network.ftth.lazy.tsx.
export const Route = createFileRoute('/_auth/network/ftth')({})

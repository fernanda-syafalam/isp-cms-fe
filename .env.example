# Public env vars must be prefixed with VITE_. Never put secrets here.
# See CLAUDE.md > Security.

# Mock-first phase: keep this at /api so the dev MSW worker intercepts requests
# (see docs/ADR/0003-isp-modules-mock-first.md). Point it at the real backend
# (e.g. http://localhost:3000/v1) once endpoints exist.
VITE_API_BASE_URL=/api
VITE_APP_NAME=isp-cms
